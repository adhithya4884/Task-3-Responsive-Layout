# Task-3-Responsive-Layout

Description: Design a webpage with four sections: a header, a sidebar, a main content area, and a footer. You must use CSS to make the layout responsive so it looks good on all screen sizes. This involves applying media queries to switch from a grid layout (for desktops) to a stacked layout (for mobile). 

Skills: HTML, CSS
